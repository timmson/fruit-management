FROM php:5.6-apache

RUN docker-php-ext-install mysql calendar && docker-php-ext-enable mysql calendar

COPY src/ /var/www/html/

RUN chown www-data:www-data -R /var/www/html && chmod 744 -R /var/www/html

